[project]
name = "calculator-app"
version = "1.0.0"
description = "A natural-language, fraction-aware, meme-enabled calculator."
requires-python = ">=3.10"

packages = ["calc_app"]

[project.scripts]
calc-app = "app:main"
